#include<stdio.h>
int main()
{
	int i=1;
	int sum_even,sum_odd;
	while(i<=10){
		if(i%2==0){
			sum_even+=i;
		} else {
			sum_odd+=i;
		}
		printf("%d",i);
		i++;
}
printf("sum of even numbers %d",sum_even);
printf("sum of odd numbers %d",sum_odd);
}
